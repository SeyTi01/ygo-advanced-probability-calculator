<Window x:Class="YGOProbabilityCalculator.Views.Dialogs.CategorySelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Categories" Height="400" Width="300">
    <StackPanel Margin="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300">
            <ItemsControl x:Name="CategoriesItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Margin="0,2,0,2">
                            <CheckBox.IsChecked>
                                <Binding Path="IsChecked" Mode="TwoWay"/>
                            </CheckBox.IsChecked>
                            <TextBlock Text="{Binding Name}"/>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="75" Margin="0,0,5,0"
                    Click="OkButton_Click"/>
            <Button Content="Cancel" Width="75"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>