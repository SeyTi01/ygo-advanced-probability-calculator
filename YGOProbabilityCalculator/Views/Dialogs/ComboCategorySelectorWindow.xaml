<Window x:Class="YGOProbabilityCalculator.Views.Dialogs.ComboCategorySelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Categories" Height="400" Width="400">
    <StackPanel Margin="10">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Categories}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" 
                                    Content="{Binding Category}"
                                    IsChecked="{Binding IsSelected}" 
                                    VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1" 
                                   Text="{Binding MinCount}" 
                                   Margin="5,0"
                                   IsEnabled="{Binding IsSelected}"/>
                            <TextBox Grid.Column="2" 
                                   Text="{Binding MaxCount}"
                                   Margin="5,0"
                                   IsEnabled="{Binding IsSelected}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="OK" Width="75" Margin="0,0,5,0" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="75" Click="CancelButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
